services:
  gateway:
    ports:
      - "${FOREST_GATEWAY_HTTPS_PORT:-443}:443"
    volumes:
      - ${FOREST_GATEWAY_SSL_DIR:?set FOREST_GATEWAY_SSL_DIR to your local ssl directory}:/etc/nginx/ssl:ro
