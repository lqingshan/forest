import type { ThemeId } from '@forest/ui-kit/theme'

export const appConfig: { appCode: string; clientType: string; accessScope: string; themeId: ThemeId } = {
  appCode: 'cxc-commerce-merchant-web',
  clientType: 'PC_WEB',
  accessScope: 'ADMIN',
  themeId: 'figma'
}
